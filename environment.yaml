name: MOZAIC
channels:
  - conda-forge
dependencies:
  - python=3.9
  - openbabel
  - pip
  - pip:
      - rdkit
      - tqdm
      - meeko
      - scipy
      - gemmi
      - pandas
      - pyyaml
      - openpyxl
      - matplotlib
      - vina
