name: MOZAIC
channels:
  - conda-forge

dependencies:
  - python=3.11
  - openbabel
  - vina
  - pip
  - pip:
      - rdkit
      - tqdm
      - meeko
      - scipy
      - gemmi
      - pandas
      - pyyaml
      - openpyxl
